version: 2

# Build documentation in the docs/ directory with Sphinx
sphinx:
   configuration: client/docs/conf.py
   
# Optionally declare the Python requirements required to build your docs
python:
   install:
   - requirements: client/docs/requirements.txt